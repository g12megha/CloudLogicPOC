'use strict';

var logoutProvider = require('./logout-provider.js');

module.exports = function(req, res, next) {
  if (req.method === 'GET') {
    return logoutProvider.centralLogout(req, res);
  }
  next();
};
