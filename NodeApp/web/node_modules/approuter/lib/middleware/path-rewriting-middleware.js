'use strict';

var pathRewriter = require('./path-rewriter.js');

module.exports = function pathRewrite(req, res, next) {
  req.internalUrl = pathRewriter(req, req.app.get('routerConfig'));
  next();
};
