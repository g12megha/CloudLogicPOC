'use strict';

var http = require('http');

module.exports = function urlCheck(req, res, next) {
  if (!req.internalUrl) { // no route matches the request path
    res.writeHead(404);
    res.end(http.STATUS_CODES[404]);
  } else {
    next();
  }
};
