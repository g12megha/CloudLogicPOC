'use strict';

var handler = require('./welcome-page-handler.js');

module.exports = function welcomePage(req, res, next) {
  var welcomeFile = handler.getWelcomeFile(req);
  if (welcomeFile) {
    res.writeHead(302, { location: welcomeFile});
    res.end();
  } else {
    next();
  }
};
